experiment_name: "default"

grid_size: 3
start_position: 4
goal_position: 6
n_steps: 10

neuron:
  learning_rate: 1
  wmin: 0.001
  wmax: 1
  weight_decay: 0
  post_spike_weight_decay: 0.005
  reset: 0
  refrac: 40
  thresh: 7

astrocyte:
  enable: true
  alpha: 0.001
  k: 0.2

simulation:
  intensity: 15.0
  time_steps: 1000
  dt: 1

experiment:
  num_experiments: 2
#  seeds: [42, 223]
  num_cycles: 2

output_dir: "results"
logger_type: "file"